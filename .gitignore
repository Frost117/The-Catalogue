# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Cloudflare/wrangler local state (local D1 emulation, etc.)
.wrangler

# Node dependencies
node_modules

# This project uses pnpm (see packageManager in package.json) — never commit
# a lockfile from a different package manager.
package-lock.json
yarn.lock

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# wrangler pages dev runtime env (contains secrets)
.dev.vars

# API-client collections (contain secrets)
*.postman_collection.json
